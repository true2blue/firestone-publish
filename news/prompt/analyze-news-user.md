请从以下网页内容中提取所有新闻条目，并按照指定格式输出。

提取要求：
1. 识别网页中所有新闻标题和对应的链接
2. 只提取确认为新闻的内容，排除广告、导航菜单等其他非新闻内容
3. 确保链接是完整的URL（如果是相对路径，请转换为绝对路径）

输出格式要求：
- 使用标准的JSON格式
- 每个数组元素是一个对象，包含：
  * "type": 新闻来源，无法识别时可写"未知" (字符串) 
  * "content": 新闻标题（字符串）
  * "url": 新闻链接（字符串）
- 如果没有找到新闻，返回空数组

示例输出：
[
    {
        "type" : "CNBC",
        "content": "示例新闻标题1",
        "url": "https://example.com/news/1"
    },
    {
        "type" : "华尔街见闻",
        "content": "示例新闻标题2",
        "url": "https://example.com/news/2"
    }
]


请处理以下网页内容：
${HTML_CONTENT}